services:
  # --------------------------------------------------------
  # 1. PostgreSQL (with pgvector for VectorDB & Core DB)
  # --------------------------------------------------------
  postgres:
    image: pgvector/pgvector:pg16
    container_name: dsp-postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${CORE_DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker-init/init-dbs.sql:/docker-entrypoint-initdb.d/init-dbs.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # --------------------------------------------------------
  # 2. Redis (Caching & Queues)
  # --------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: dsp-redis
    restart: always
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # --------------------------------------------------------
  # 3. Kafka (Event Streaming - KRaft Mode)
  # --------------------------------------------------------
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: dsp-kafka
    restart: always
    ports:
      - "${KAFKA_BROKER_PORT}:9092"
      - "${KAFKA_CONTROLLER_PORT}:9093"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:${KAFKA_BROKER_PORT}'
      KAFKA_LISTENERS: 'PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093,PLAINTEXT_HOST://0.0.0.0:${KAFKA_BROKER_PORT}'
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      CLUSTER_ID: 'ciWo7IWazngRchmPES6q5A=='
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-server localhost:${KAFKA_BROKER_PORT} > /dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s

  # --------------------------------------------------------
  # 4. Elasticsearch (CQRS & Text Search)
  # --------------------------------------------------------
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.10.2
    container_name: dsp-elasticsearch
    restart: always
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
    ports:
      - "${ELASTIC_PORT}:9200"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf -u elastic:$${ELASTIC_PASSWORD} http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 60s

  # --------------------------------------------------------
  # 4b. ES log-routing + RBAC bootstrap (one-shot, idempotent)
  # --------------------------------------------------------
  elasticsearch-setup:
    image: curlimages/curl:8.10.1
    container_name: dsp-elasticsearch-setup
    environment:
      ELASTICSEARCH_URL: http://elasticsearch:9200
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD}
      DSP_OPS_READER_PASSWORD: ${DSP_OPS_READER_PASSWORD}
      DSP_AUDIT_READER_PASSWORD: ${DSP_AUDIT_READER_PASSWORD}
      KIBANA_SYSTEM_PASSWORD: ${KIBANA_SYSTEM_PASSWORD}
    volumes:
      - ./docker-init/elasticsearch:/es-init:ro
    entrypoint: ["/bin/sh", "/es-init/setup.sh"]
    depends_on:
      elasticsearch:
        condition: service_healthy
    restart: "no"

  # --------------------------------------------------------
  # 5. Kafka UI
  # --------------------------------------------------------
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: dsp-kafka-ui
    profiles: ["monitoring"]
    ports:
      - "${KAFKA_UI_PORT}:8080"
    environment:
      - KAFKA_CLUSTERS_0_NAME=local
      - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:29092
      - AUTH_TYPE=LOGIN_FORM
      - SPRING_SECURITY_USER_NAME=${KAFKA_UI_USER}
      - SPRING_SECURITY_USER_PASSWORD=${KAFKA_UI_PASSWORD}
    depends_on:
      kafka:
        condition: service_healthy

  # --------------------------------------------------------
  # 6. RedisInsight
  # --------------------------------------------------------
  redisinsight:
    image: redis/redisinsight:latest
    container_name: dsp-redisinsight
    profiles: ["monitoring"]
    depends_on:
      redis:
        condition: service_healthy

  # --------------------------------------------------------
  # 7. Kibana (Elasticsearch UI)
  # --------------------------------------------------------
  kibana:
    image: docker.elastic.co/kibana/kibana:8.10.2
    container_name: dsp-kibana
    profiles: ["monitoring"]
    ports:
      - "${KIBANA_PORT}:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      # kibana_system, NOT elastic — ES 8.x refuses to boot Kibana against the
      # superuser account (real crash-loop bug found + fixed 2026-07-25).
      # Password set by docker-init/elasticsearch/setup.sh.
      - ELASTICSEARCH_USERNAME=kibana_system
      - ELASTICSEARCH_PASSWORD=${KIBANA_SYSTEM_PASSWORD}
    depends_on:
      elasticsearch:
        condition: service_healthy

  # --------------------------------------------------------
  # 8. Local API Gateway (Nginx)
  # --------------------------------------------------------
  api-gateway:
    image: nginx:alpine
    container_name: dsp-api-gateway
    restart: always
    ports:
      - "${API_GATEWAY_PORT}:80"
      - "${REDISINSIGHT_PORT}:8001"
      - "${PROMETHEUS_PORT}:9090"
    volumes:
      - ./docker-init/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker-init/.htpasswd:/etc/nginx/.htpasswd:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # --------------------------------------------------------
  # 9. Prometheus (Metrics Collector)
  # --------------------------------------------------------
  prometheus:
    image: prom/prometheus:latest
    container_name: dsp-prometheus
    profiles: ["monitoring"]
    volumes:
      - ./docker-init/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./docker-init/prometheus/rules.yml:/etc/prometheus/rules.yml
      - prometheus_data:/prometheus
    restart: always

  # --------------------------------------------------------
  # 10. Grafana (Metrics Dashboard)
  # --------------------------------------------------------
  grafana:
    image: grafana/grafana:latest
    container_name: dsp-grafana
    profiles: ["monitoring"]
    ports:
      - "${GRAFANA_PORT}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./docker-init/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus
    restart: always

  # --------------------------------------------------------
  # 11. Node Exporter (Host Metrics)
  # --------------------------------------------------------
  node-exporter:
    image: prom/node-exporter:latest
    container_name: dsp-node-exporter
    profiles: ["monitoring"]
    ports:
      - "${NODE_EXPORTER_PORT}:9100"
    restart: always


  # --------------------------------------------------------
  # 12. Postgres Exporter
  # --------------------------------------------------------
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: dsp-postgres-exporter
    profiles: ["monitoring"]
    environment:
      DATA_SOURCE_NAME: "postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${CORE_DB_NAME}?sslmode=disable"
    ports:
      - "${POSTGRES_EXPORTER_PORT}:9187"
    depends_on:
      - postgres
    restart: always

  # --------------------------------------------------------
  # 13. Redis Exporter
  # --------------------------------------------------------
  redis-exporter:
    image: oliver006/redis_exporter:latest
    container_name: dsp-redis-exporter
    profiles: ["monitoring"]
    environment:
      REDIS_ADDR: "redis:6379"
    ports:
      - "${REDIS_EXPORTER_PORT}:9121"
    depends_on:
      - redis
    restart: always

  # --------------------------------------------------------
  # 14. Kafka Exporter
  # --------------------------------------------------------
  kafka-exporter:
    image: danielqsj/kafka-exporter:latest
    container_name: dsp-kafka-exporter
    profiles: ["monitoring"]
    command: ["--kafka.server=kafka:29092"]
    ports:
      - "${KAFKA_EXPORTER_PORT}:9308"
    depends_on:
      - kafka
    restart: always

  # --------------------------------------------------------
  # 15. Elasticsearch Exporter
  # --------------------------------------------------------
  elasticsearch-exporter:
    image: prometheuscommunity/elasticsearch-exporter:latest
    container_name: dsp-elasticsearch-exporter
    profiles: ["monitoring"]
    command: ["--es.uri=http://elasticsearch:9200"]
    ports:
      - "${ELASTICSEARCH_EXPORTER_PORT}:9114"
    depends_on:
      - elasticsearch
    restart: always

  # --------------------------------------------------------
  # 16. Nginx Exporter
  # --------------------------------------------------------
  nginx-exporter:
    image: nginx/nginx-prometheus-exporter:latest
    container_name: dsp-nginx-exporter
    profiles: ["monitoring"]
    command: ["-nginx.scrape-uri=http://api-gateway:80/stub_status"]
    ports:
      - "${NGINX_EXPORTER_PORT}:9113"
    depends_on:
      - api-gateway
    restart: always

  # --------------------------------------------------------
  # 18. FluentBit (production log collector) — BLUEPRINT, not started by
  #     default (profile "prod-logging-blueprint"). apps/* have no
  #     Dockerfile yet, so there's no real container log to tail — see
  #     docker-init/fluent-bit/fluent-bit.conf header + logging_standard.md
  #     "Production log shipping" section before wiring this for real.
  # --------------------------------------------------------
  fluent-bit:
    image: fluent/fluent-bit:3.1
    container_name: dsp-fluent-bit
    profiles: ["prod-logging-blueprint"]
    environment:
      ELASTIC_PASSWORD: ${ELASTIC_PASSWORD}
    volumes:
      - ./docker-init/fluent-bit:/fluent-bit/etc:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    depends_on:
      elasticsearch:
        condition: service_healthy

  # --------------------------------------------------------
  # 17. Embedding Service (self-hosted, Cortex Phase 4 RAG)
  #     Ollama serving nomic-embed-text (dim 768). Claude has NO embeddings API —
  #     vectors come from here, not Anthropic. After up, pull the model once:
  #       docker exec dsp-embedding ollama pull nomic-embed-text
  #     Batch API: POST /api/embed {"model":"nomic-embed-text","input":[...]}
  #       -> {"embeddings":[[...768...], ...]}
  # --------------------------------------------------------
  embedding:
    image: ollama/ollama:latest
    container_name: dsp-embedding
    restart: always
    ports:
      - "${EMBEDDING_SERVICE_PORT}:11434"
    volumes:
      - embedding_data:/root/.ollama

volumes:
  postgres_data:
  redis_data:
  kafka_data:
  es_data:
  prometheus_data:
  grafana_data:
  embedding_data:
