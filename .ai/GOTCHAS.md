# 🔥 Known Gotchas & Lessons Learned (from memory)

> **Auto-generated by `.ai/knowledge_builder.py`** — do not hand-edit; append to
> `.ai/memory/*.jsonl` instead (canonical entry shape: `directives/memory_sop.md`).
> Last updated: 2026-08-11T15:19:48.165880+00:00
>
> Real problems hit during development. **Search this BEFORE debugging** to avoid repeating them.
> Split out of `KNOWLEDGE_INDEX.md` on 2026-08-07: it was 63% of that file's tokens while being
> needed only for debugging, and the index is read at every session start.
> All 170 entries shown; bodies are clipped — full text in `.ai/memory/*.jsonl`.

- **db_transient_error_total counter was blind to CreditConcurrencyError-style retries: isTransient() and recordObservation() had different scopes** `[packages/shared-kernel/src/resilience/prisma-transient-error.ts, prisma-transient-error.spec.ts]`
  → User asked whether P2002 needs monitoring alongside the tracked P2034/P2028 (packages/shared-kernel/src/resilience/prisma-transient-error.ts). Answer for raw Prisma P2002 was no (deliberately excluded per ADR-0001 9b#6, business errors would pollute a DB-reliability metric) -- b…
- **CLAUDE.md was missing decision-critical content from AGENTS.md (Task Classification table, Citation Protocol) — fixed by duplicating in full, plus a warn-only…** `[CLAUDE.md, AGENTS.md, scripts/sync.cjs]`
  → User pushed back on the earlier claim that CLAUDE.md-mirrors-AGENTS.md duplication was harmless: confirmed empirically that Claude Code auto-loads ONLY CLAUDE.md at session start (the very first system-reminder of every session in this project contains CLAUDE.md verbatim, never…
- **PROJECT_STATUS.md described ADR-0001/TxScope/saga/correlation-id as "uncommitted, pending review" for a week after it was actually committed** `[.ai/PROJECT_STATUS.md, scripts/sync.cjs]`
  → User asked whether the per-turn hook routing line was even worth it, which led to auditing sync.cjs After-Task discipline check (scripts/sync.cjs:211-226). Found two real gaps: (1) codeFiles.length===0 short-circuits the whole check, so once code is committed the discipline chec…
- **Both hooks now descend only into submodules the root git status already flags — per-turn overhead 4.1s -> 2.5s** `[.claude/hooks/turn-context.cjs, scripts/sync.cjs]`
  → End-to-end verification pass of the whole AI workflow (running each mechanism and inspecting its artefact, not its exit status). Everything passed, but timing showed turn-context.cjs at 1624ms and sync.cjs at 2511ms -- ~4.1s added to every single turn, which the user feels as la…
- **load_jsonl swallowed malformed memory lines in silence -- an After-Task entry was written, reported done, and discarded** `[.ai/knowledge_builder.py load_jsonl, .ai/memory/architecture.jsonl]`
  → Surfaced only because the user asked what the '7 schemas' were and the measuring script crashed on a JSONDecodeError. The offending line was architecture.jsonl:65 -- the After-Task entry written one turn earlier, containing a regex whose backslashes were not doubled for JSON (\.…
- **Repurposed UserPromptSubmit hook from static-rule reminder to turn-local STATE; made After-Task Protocol blocking; fixed submodule blindness that made the disc…** `[.claude/hooks/turn-context.cjs, scripts/sync.cjs, .claude/settings.json, AGENTS.md, .ai/KNOWLEDGE_ARCHITECTURE.md, SETUP.md, docs/09_devops_infrastructure.md]`
  → Three linked changes after the user asked whether the per-turn hook was even worth keeping, given it restates content already in CLAUDE.md. (1) HOOK REPURPOSED: doc-select.cjs -> turn-context.cjs. Evidence for the change was empirical, not theoretical -- once the systemMessage->…
- **Split gotcha buffer out of KNOWLEDGE_INDEX.md into on-demand .ai/GOTCHAS.md -- mandatory session-start read cut 21.3k -> 8.0k tokens** `[.ai/knowledge_builder.py, AGENTS.md, CLAUDE.md, .ai/KNOWLEDGE_ARCHITECTURE.md, directives/memory_sop.md, .claude/hooks/doc-select.cjs]`
  → User asked what the AI-workflow files actually cost per prompt. Measured instead of estimating: index 72,405 B ~= 21,295 tokens, and a per-section breakdown showed section 4 (gotchas) alone was 45,534 B = 13,392 tokens = 63% of the file. That section is a 'have I hit this before…
- **Memory JSONL collapsed to ONE canonical shape {timestamp,type,title,detail,context}; builder now normalizes all 7 legacy shapes** `[.ai/knowledge_builder.py, scripts/sync.cjs, directives/memory_sop.md, AGENTS.md]`
  → Found while verifying the doc-select hook fix: a freshly appended memory line did not appear in KNOWLEDGE_INDEX.md section 4. Root cause in knowledge_builder.py -- the renderer required BOTH `error` and `solution` (if error and solution:), but 161 entries had drifted into 7 diff…
- **doc-select UserPromptSubmit hook used systemMessage -- its reminder never reached the agent (2026-08-07)** `[gotchas]`
  → User asked whether this project's AI-support files were actually correct. Auditing the mechanical layer (the only part that is enforced rather than merely written down) found .claude/hooks/doc-select.cjs emitting JSON.stringify({systemMessage: msg}). Verified against the officia…
- **Claude/Gemini summarizer had breakers but no bounded per-call timeout (2026-08-04)** `[gotchas]`
  → Continued the same-day *.caller.ts audit (fetch()/breaker bug fix) one level further: checked whether GeminiSummarizer and ClaudeSummarizer had the SAME per-call latency bound every other hot-path external call in this service already has (Ollama embedding 5s via AbortSignal, El…
- **fetch() does not reject on HTTP error status -- GeminiSummarizer/HttpEmbeddingService breaker never saw real failures (2026-08-04)** `[gotchas]`
  → User asked to audit ALL *.caller.ts SRP wrappers for the same class of layering bug found in AuthProvisioningClient. Found a real but OPPOSITE-direction bug in GeminiSummarizer (gemini-summarizer.ts) and HttpEmbeddingService (http-embedding.service.ts, Ollama): both passed a bar…
- **AuthProvisioningClient threw an ApplicationError from the infra layer instead of the application handler (2026-08-04)** `[gotchas]`
  → User reviewed AuthProvisioningClient.provisionUser and caught that even though the earlier 2026-07-11 audit correctly separated ALREADY_EXISTS from the circuit breaker (tagged resolve instead of reject, so business outcomes don't trip the breaker), it left a SEPARATE layering bu…
- **DLQ replay switched fixed 60s delay to full-jitter exponential backoff (2026-08-04)** `[architecture]`
  → User flagged a design smell reading the DLQ auto-replay flow: consumer already retried-then-failed, and DlqReplayConsumer was replaying after a FIXED 60s delay regardless of how many times a message had already been replayed -- functionally just 'retry again after a pause', not…
- **Documented IMessagePublisher's Publisher-vs-Producer naming + moved events/ inside messaging/ (2026-08-01)** `[architecture]`
  → Two small follow-ups from the same naming-readability discussion. (1) Added a doc comment to IMessagePublisher (message-publisher.interface.ts) explaining WHY it's named 'Publisher' not 'Producer': it's deliberately transport-agnostic (could resolve to Kafka, queue, or both via…
- **Renamed MinimalDlqProducer -> MinimalProducer, createDlqProducer() -> createProducer() (2026-08-01)** `[conventions]`
  → User's follow-up catch right after the adapter-class collapse: MinimalDlqProducer's shape (send/connect/disconnect) has nothing DLQ-specific about it -- same asymmetry as before, MinimalConsumer is named generically (shared by both normal and DLQ consumer flows) while its produc…
- **Collapsed KafkaConsumerAdapter/KafkaDlqProducerAdapter into type-cast one-liners (2026-08-01)** `[architecture]`
  → User questioned whether KafkaClientService's createConsumer/createDlqProducer wrapper classes were necessary at all, since their own doc comments already admitted 'this class adds no new behavior over passing kafka.consumer(config) directly' -- a raw kafkajs Consumer/Producer al…
- **Reverted the at-least-once-loop.ts extraction (2026-08-01, same-day revert)** `[architecture]`
  → User reconsidered right after seeing the file list for the ResilientEventConsumer/DlqReplayConsumer shared-loop extraction: 'thay khong co tac dung gi may back lai di' (doesn't seem to do much, revert it). Reverted cleanly: deleted messaging/at-least-once-loop.ts, restored resil…
- **Extracted shared at-least-once-loop.ts for ResilientEventConsumer + DlqReplayConsumer (2026-08-01)** `[architecture]`
  → User asked whether ResilientEventConsumer and DlqReplayConsumer (packages/shared-kernel/src/messaging/) were similar enough to reuse. Verdict on analysis: yes, real (if modest) duplication -- both had an identical connect/subscribe/run({autoCommit:false,eachMessage})/disconnect…
- **Removed unused error param from ScheduledJobRegistry.recordFailure instead of prefixing with _ (2026-07-31)** `[conventions]`
  → User caught recordFailure(name, _error: unknown) accepting a parameter it never used inside the method body (Gauge/Counter dont touch the error value at all -- Prometheus labels should stay bounded-cardinality, a raw error message doesnt belong there, and each job already logs i…
- **Flattened infrastructure/outbox/ + replaced GET /jobs with a Prometheus info metric (2026-07-31, 2 more same-day corrections)** `[architecture]`
  → User caught 2 more real issues right after the scheduled-jobs work landed. (1) infrastructure/outbox/ kept its old domain/repositories + infrastructure/{cleanup,publishers,reapers,reporters,repositories} nested structure from when it lived under modules/outbox/ (DDD-style module…
- **Corrected ScheduledJobRegistry: moved live-health from in-memory to Prometheus (2026-07-31, same-day fix of the just-built job registry)** `[architecture]`
  → User challenged the ScheduledJobRegistry design immediately after it was built, asking 'has anyone actually done it this way in practice' -- and was right to. Two real design flaws, not just a different opinion: (1) in-memory health state directly contradicts this app's own alre…
- **Built infrastructure/scheduled-jobs/ (ScheduledJobRegistry + GET /jobs) across all 7 core-api background jobs (2026-07-31)** `[architecture]`
  → User's own instinct ('cam giac khong kiem soat het roi') confirmed by hard evidence: 7 job classes / 8 @Cron+@Interval ticks now exist, matching the tripwire resilience_patterns.md SS6 itself pre-declared (~6-8 jobs -> build a real registry). The hand-maintained markdown table h…
- **Moved core-api's modules/outbox/ to infrastructure/outbox/ (2026-07-31)** `[architecture]`
  → User caught a real placement asymmetry while reading the 2 new cleanup services just added: outbox lived under modules/outbox/ (full domain/infrastructure module skeleton) while saga-compensation -- functionally the same kind of thing, a generic cross-cutting reliability mechani…
- **Added nightly cleanup for outbox_events + saga_compensations PROCESSED/DONE rows (2026-07-31)** `[architecture]`
  → User confirmed understanding that outbox/saga_compensation Postgres tables ARE the durable secondary store (unlike Kafka topics, which expire via retention) -- then asked whether they have a cleanup mechanism. Checked: they did not (grep for purge/cleanup/archive/deleteMany in b…
- **Closed the outbox observability gap: metrics + recording rule + alert (2026-07-31)** `[architecture]`
  → User explicitly asked to fix this gap now (deferred the separate DLQ-secondary-store idea for later). Before: core-api had zero outbox-specific Prometheus metrics (only collectDefaultMetrics() Node process stats), and a row crossing into permanent FAILED_DLQ logged the exact sam…
- **Backlog (not built): DLQ give-up secondary store + outbox observability gap (2026-07-31)** `[architecture]`
  → Discussion-only, deferred by user ('note lai trien khai sau nay'). Two related findings while reviewing DlqReplayConsumer's onGiveUp path: (1) On permanent give-up (replayCount >= maxReplays), the message is left in the .DLQ Kafka topic bounded only by topic retention -- no dura…
- **Paid off the auth-service I-prefix debt: renamed all 8 non-I-prefixed interfaces (2026-07-31)** `[conventions]`
  → Follow-up to the repo-wide interface naming audit: user explicitly asked to fix the known auth-service exception now instead of leaving it as documented debt. Renamed across 53 files: UserRepository->IUserRepository, RoleRepository->IRoleRepository, RefreshTokenRepository->IRefr…
- **Messaging Port naming inconsistent: DeadLetterPort had no I-prefix while IMessagePublisher/ITransportPublisher/IIntegrationEventHandler in the same folder all…** `[conventions]`
  → User read through the messaging/interfaces/ folder as part of reviewing the publisher/consumer/DLQ-replay flows and asked point-blank whether interfaces follow any naming rule, since some had I-prefix and one didn't. naming_conventions.md had rules for Repository (SS4, I{Entity}…
- **Renamed DeadLetterPort to IDeadLetterProducer for naming consistency (2026-07-31)** `[architecture]`
  → User caught a real naming inconsistency while asking me to explain what DeadLetterPort is: every sibling port in messaging/interfaces/ uses I{Noun} (IMessagePublisher, ITransportPublisher, IIntegrationEventHandler) but this one used {Noun}Port with no I-prefix -- an odd one out,…
- **Renamed shared-kernel messaging/kafka/ to messaging/kafka-shapes/ + documented the 3-axis split in index.ts (2026-07-31)** `[architecture]`
  → Follow-up to the app-level adapter merge same day: user pushed back on my initial defense of messaging/'s subfolder layout ('ban thay hien tai to chuc folder nay on roi a' -- pointed tone, not agreement). Re-examined honestly instead of defending prior work: routing/, interfaces…
- **Merged kafka-consumer.adapter.ts + kafka-dlq-producer.adapter.ts back into kafka-client.service.ts (2026-07-31, reverses part of the same-day adapter-landmark…** `[architecture]`
  → User's own rule, stated directly: if A needs B and C exclusively (A is the only constructor of B/C), put classes A/B/C in one file -- splitting for its own sake with no second consumer is just scatter. Applied to KafkaClientService (A) + KafkaConsumerAdapter (B) + KafkaDlqProduc…
- **DLQ-replay producer made idempotent for consistency (2026-07-31, closes the open item from the Kafka adapter collapse)** `[architecture]`
  → KafkaDlqProducerAdapter (both notification-service and search-service) now calls kafka.producer({idempotent:true, maxInFlightRequests:5}), matching DeadLetterProducer and the outbound KafkaProducerService -- previously it was the only one of the 3 producers in the system without…
- **Collapsed Kafka adapter construction into KafkaClientService factory methods (2026-07-31, follow-up to the adapter-landmark change same day)** `[architecture]`
  → User's initialization-flow diagram review (both notification-service and search-service) found the adapter classes added earlier the same day were correct but still meant 3 separate imports + 3 `new` calls per consumer file (KafkaClientService, KafkaConsumerAdapter, KafkaDlqProd…
- **messaging/ folder reorg (shared-kernel) + explicit Kafka adapter classes in notification-service/search-service (2026-07-31)** `[architecture]`
  → shared-kernel/src/messaging/ mixed 3 abstraction levels in the same 4 files (ports, kafkajs-shaped structural types, concrete classes) with no folder convention, unlike cqrs/ which already separates interfaces/ from impl. Reorged to interfaces/ (message-publisher, event-handler,…
- **Saga reliability follow-up (2026-07-30): SagaCompensationOutbox + DLQ auto-reprocessor + idempotent ProvisionUser gRPC call, built on top of the ADR-0001 remed…** `[Follow-up to ADR-0001 post-implementation review, same session, same day (2026-07-30). Touched packages/shared-kernel/src/{cqrs/{command-bus.ts,interfaces/saga-context.interface.ts,interfaces/saga-compensation-store.interface.ts (new)},messaging/dlq-replay-consumer.ts (new)}; apps/core-api/{prisma/schema.prisma (SagaCompensation model),src/infrastructure/saga-compensation/ (new: repository, registry, reaper, module),src/modules/platform-admin/ (ArchiveOrgCommand compensation wiring, controller X-Idempotency-Key extraction)}; apps/auth-service/{prisma/schema.prisma (GrpcIdempotencyRecord model, User.provisionedViaSaga column),src/modules/auth/domain/repositories/grpc-idempotency.repository.ts (new),src/modules/auth/application/commands/provision-user/ (rewritten),src/modules/user/infrastructure/jobs/orphaned-provisioned-user-watcher.ts (new)}; apps/notification-service and apps/search-service (dlq-replay.consumer.ts new in each, new env vars, new prom-client metrics); proto/org-provisioning.proto (idempotency_key field, regenerated). Plan + full rationale: .ai/plans/saga-compensation-outbox.plan.md. npx turbo typecheck test: 19/19 tasks green. NOT yet runnable end-to-end -- schema changes only got prisma generate (client codegen), nobody ran npm run db:push against a live Postgres in this session (none was running).]`
  → Discussing the ADR-0001 review surfaced a gap the review itself had flagged but not fixed: CommandBus.runSaga logged a failed compensation and moved on -- no durable store, so a failed cancel-provisioned-user or archive-org retry had nowhere to be picked up again. Three pieces c…
- **ADR-0001 post-implementation review (2026-07-30) found 10 execution gaps in an otherwise-sound design; all fixed same day, ADR §9b amended, remediation plan at…** `[Post-implementation architecture review of ADR-0001 (docs/adr/0001-transaction-retry-boundary.md), triggered by /code-review on the uncommitted diff; remediation touched packages/shared-kernel/src/{cqrs/command-bus.ts,cqrs/errors/cqrs.error.ts,cqrs/interfaces/command-handler.interface.ts,database/abstract-tx-runner.ts (new),database/tx-scope.ts,messaging/event-router.ts,resilience/prisma-transient-error.ts (new)} + all 5 services (core-api: archive-org command, credit.errors.ts marker, prisma-tx-runner.ts, tx-scope factories; auth-service: login/register handler afterCommit, prisma-tx-runner.ts, tx-scopes.ts; notification-service + search-service: event handler txScope + consumer OnApplicationBootstrap wiring, prisma-tx-runner.ts; worker-service: deleted dead prisma-tx-runner.ts).]`
  → Independent 8-angle review of the just-implemented TxScope/CommandBus change (correctness, removed-behavior, cross-file, concurrency/retry, reuse/simplification, efficiency, altitude/ADR-fidelity, CLAUDE.md conventions) found the CORE DESIGN was sound (TxScope-as-value, handler-…
- **ADR-0001 IMPLEMENTED across all 5 packages: TxScope Unit-of-Work replaces AsyncLocalStorage-implicit transaction, handler TYPE replaces the transactional flag,…** `[ADR-0001 implementation; shared-kernel cqrs + database, all 4 services + worker-service; directives/cqrs_pattern.md rewritten, eventing_patterns.md §4.3 updated]`
  → Owner skipped the planned notification-service pilot and asked to apply the architecture to every service at once. Scope: 38 command handlers, 26 repositories, 3 different composition roots, 8 TxScopes (Knowledge/Engagement/Tenant/Credit in core-api, Auth/Rbac/User in auth-servi…
- **ADR-0001 ACCEPTED: thay AsyncLocalStorage-implicit-transaction bang Unit-of-Work phoi repository + suy tu chu ky handler + fail-fast luc boot. Code CHUA migrat…** `[docs/adr/0001-transaction-retry-boundary.md; anh huong CommandBus + moi command handler + moi write repository o 4 service; supersede mot phan directives/cqrs_pattern.md §5 sau khi pilot xong]`
  → Bat nguon tu 1 cau hoi cua user trong buoi hoc RAG: 'event handler ghi nhieu repo cung 1 DB thi lam sao, chang le import prisma vao handler'. Dieu tra ra 6 lo hong ma moi bat bien deu chi duoc bao ve bang comment/ky luat/lint, khong cai nao duoc type hay cau truc cuong che: (1)…
- **Transaction seam bi bo qua o notification-service/search-service: TransactionMiddleware wired nhung repo ghi thang this.prisma.client -> transaction chay ma kh…** `[notification-service + search-service repositories tx-awareness, ITransactionManager la port doc lap khong thuoc CQRS, eslint no-restricted-syntax forcing-function; directives/cqrs_pattern.md §5 + eventing_patterns.md §4.3]`
  → Phat hien khi user (dang hoc RAG, doc code search-service) hoi: 'event handler hoan toan co the thao tac nhieu repo trong cung 1 database, khong co transaction toan cuc thi lam sao, chang le import prisma vao handler (pha kien truc ngay)?'. Dieu tra: (1) ITransactionManager/runI…
- **Domain Port naming inconsistent: embedding.service.ts (IEmbeddingService) vs summarizer.ts (ISummarizer) — same architectural role (outbound port to AI provide…** `[search-service domain/services/ port naming, directives/naming_conventions.md §9 (new)]`
  → User (learning RAG, first read-through of search-service code) asked why 3 files in domain/services/ (embedding.service.ts, summarizer.ts, text-chunker.ts) had inconsistent naming. Diagnosis: text-chunker.ts is correctly unsuffixed — it's a pure domain service with no interface…
- **Correlation-id propagation (W3C traceparent) added across HTTP/gRPC/Kafka — last item of the 5-part resilience curriculum** `[directives/resilience_patterns.md updated with a new section documenting this. TRIPWIRE for whoever picks this up next: OutboxEvent.traceparent column exists in schema.prisma and Prisma client was regenerated, but npm run db:push was NOT run (no Postgres running at implementation time) — must run apps/core-api db:push before this is live, otherwise PrismaOutboxRepository.append() will throw on the unknown column. Completes the 5-part resilience curriculum (idempotency/rate-limit/timeout+breaker/graceful-shutdown/correlation-id) per project-roadmap-sequencing memory — next step per that sequencing is RAG curriculum Section 2+.]`
  → Before this, requestId only existed inside a single service HTTP request lifecycle (Fastify req.id / nestjs-pino per-request child logger) — a request that fanned out into a gRPC call (core-api->auth-service) or a Kafka event (outbox->consumer) could not be traced end-to-end in…
- **REVERTED the whole CommandOptions.safety apparatus -- did not match real senior practice, confirmed via audit; fixed 2 real pre-existing gaps found in the proc…** `[conventions]`
  → User asked pointedly whether real senior backend engineers, in actual practice, build the kind of thing constructed over the prior ~8-round session (a mandatory typed field on every command classifying idempotency into 5 enum values + concurrency into 3 + a formalized priority-o…
- **Documented Kafka producer-vs-consumer idempotency + dedup-constraint-vs-unique-constraint distinction in idempotency_strategy.md** `[conventions]`
  → After the deep CQRS-idempotency audit thread wrapped up, user shifted to asking about Kafka idempotency specifically (recalling shared-kernel's IIntegrationEventHandler.idempotency field mentioned earlier in the session) -- wanted to understand producer vs consumer handling. Ver…
- **Renamed CommandSafety.idempotency to primaryReplayGuard -- kept single-value structure, fixed the naming that invited misreading** `[conventions]`
  → User pushed the natural-key overlap finding to its logical conclusion: proved 4 real combos exist in the codebase (only-2 = follow-target, 1+3-no-2 = refresh, 2+3-no-1 = cast-vote, all-3 = accept-invite), then argued the type itself is semantically wrong as a mutually-exclusive…
- **CommandIdempotency values are NOT mutually-exclusive properties -- resolved via priority-order, not checklist** `[conventions]`
  → User proposed removing 'natural-key' as unnecessary, arguing it's 'just senior coding discipline' every repo method should follow, not a per-command strategic choice. Investigated the claim rigorously rather than complying or dismissing it: found the SAME argument applies equall…
- **Coverage audit of CommandSafety values: 2/8 mechanically verified, 6/8 accepted as declaration-only by choice** `[conventions]`
  → User asked directly whether, beyond occ, the rest of CommandIdempotency/CommandConcurrency values are just inert flags. Verified precisely rather than agreeing reflexively: 2/8 values have automated checks -- concurrency:'occ' (runtime, CommandSafetyMiddleware) and idempotency:'…
- **Removed retryable field entirely; RetryMiddleware now auto-retries P2034 for every transactional:true command** `[conventions]`
  → User pushed once more on RetryMiddleware value ('should we just delete Retry and CommandSafetyMiddleware too'). Instead of complying blindly, evaluated: CommandSafetyMiddleware's 2 invariants are NOT equally coupled to RetryMiddleware -- occ=>transactional stands alone (OCC alwa…
- **P2028 excluded from RetryMiddleware transient set (retry-storm risk); added onError observability hook** `[conventions]`
  → User self-critique request on RetryMiddleware surfaced a real design flaw, not just discussion: isPrismaTransientError lumped P2034 (deadlock -- Postgres auto-aborts the loser, resolves in ms, retry is the standard low-risk remediation per Prisma docs) together with P2028 (trans…
- **Self-caught scope-creep: CommandSafetyMiddleware != drift detection; added the real drift test** `[conventions]`
  → User pushed back hard and correctly: CommandSafetyMiddleware (added same session) does NOT solve the original discoverability/scatter problem -- it only cross-checks declared fields against EACH OTHER (retryable vs transactional, concurrency:'occ' vs transactional), never agains…
- **CommandSafetyMiddleware -- runtime teeth for CommandOptions.safety** `[conventions]`
  → Follow-on to the safety-field convention: gave the field real runtime consumption instead of leaving it a declaration-only flag. `CommandSafetyMiddleware` (shared-kernel/src/cqrs/middleware/command-safety.middleware.ts) is registered FIRST in the command-bus middleware chain at…
- **CommandOptions.safety -- self-declaring resilience per command (2 axes)** `[conventions]`
  → Added a MANDATORY field `safety: { idempotency, concurrency }` to CommandOptions (shared-kernel cqrs/interfaces/command.interface.ts), plus made `options` required on ICommand. Motivation: the 3 outer resilience layers (rate-limit guard, IdempotencyInterceptor, RetryMiddleware)…
- **First real unit-test coverage for core-api, notification-service, search-service — 135 tests across 52 suites, credit module explicitly excluded pending user r…** `[See gotchas.jsonl same date for the 2-part Jest config fix (moduleNameMapper + ts-jest CommonJS tsconfig override) that had to be applied to core-api, notification-service, and search-service package.json BEFORE any of these tests could run — none of the 3 services had ever had a real @CommandHandler/@QueryHandler-decorated class under test before, so the ESM/CJS shared-kernel transform bug was previously latent. Directive: directives/testing_standard.md should get a cross-reference to this fix; not yet added, follow-up if a 4th service needs the same fix.]`
  → Following the post-audit priority order in project-roadmap-sequencing (see repo owner's memory), wrote unit tests module-by-module for every core-api module except credit (AI-authored, user wants to review the code itself before trusting AI-authored tests over it) and outbox (pu…
- **shared-kernel unit test coverage added — CommandBus/QueryBus/EventBus/EventRouter/ResilientEventConsumer/middlewares/defineEvent, closing the biggest remaining…** `[Completes the 'rà lại toàn bộ module, viết unit test' step from project-roadmap-sequencing before moving to step 2 (Idempotency Interceptor + real rate-limiting). credit module and outbox infrastructure remain deliberately untested per earlier user decision. web (no test script, frontend) and worker-service/chat-service (genuinely empty scaffolds, confirmed via file listing) are not gaps -- there's nothing there to test yet.]`
  → After finishing service-level tests (core-api/notification-service/search-service, 135 tests), user asked to verify the root `npm test` (turbo run test) actually runs every service, and whether coverage was truly complete. Running it surfaced that shared-kernel -- the package ev…
- **apps/core-api/package.json's jest block had no moduleNameMapper at all (no @/ alias, no shared-kernel source-link) -- likely why core-api sat at 0 tests despit…** `[First real core-api unit tests (tenant module domain entities + 9 command/query handlers, 13 spec files / 35 tests, all passing after both fixes). Any future service (notification-service, search-service) writing its first handler test that uses @CommandHandler/@QueryHandler decorators will need the same 2-part jest config fix if it isn't already present -- check for it before assuming a genuine test-writing bug when hitting `Unexpected token 'export'`.]`
  → Two-part fix in apps/core-api/package.json jest block: (1) added the same moduleNameMapper auth-service already had (@/ alias, .js-strip, shared-kernel source-link, uuid passthrough); (2) gave the ts-jest transform an explicit tsconfig override `{ module: CommonJS, moduleResolut…
- **Monitoring stack provisioned as code: Prometheus recording rules + Grafana dashboard + 5 alert rules, all git-committed (not click-ops)** `[New directive directives/observability_monitoring.md written to hold this + a deliberate scope-boundary decision: backend IC owns WHAT to measure/alert-on and WHY (thresholds, SLO reasoning), not the provisioning YAML syntax itself (platform/SRE scope, delegate to AI/docs each time). Feeds docs/linkedin_posts_plan.md post #35 (Observability).]`
  → The `monitoring` compose profile (Prometheus/Grafana/6 exporters/Kafka UI, already existed) had a datasource but zero dashboards or alerts -- everything was manual click-through, lost on a fresh clone. Added: (1) docker-init/prometheus/rules.yml -- 8 recording rules (notificatio…
- **Added an explicit `uid: prometheus` field to docker-init/grafana/provisioning/datasources/datasource.yml (needed so dashboard/alert-rule JSON could reference t…** `[docker-init/grafana/provisioning/ setup session, see directives/observability_monitoring.md §4.2]`
  → Added `deleteDatasources: [{name: Prometheus, orgId: 1}]` at the top of datasource.yml so Grafana deletes the stale record (matched by name) before recreating it with the new fixed uid. No volume wipe needed. Rule of thumb: any time a provisioned resource's identifying field (ui…
- **auth-service System RBAC had a dynamic Permission table + POST /permissions, but SystemPermissionGuard/RequireSystemPermission only ever check a literal string…** `[System RBAC (auth-service) vs Org RBAC (core-api) design review -- decided permission catalogs should ALWAYS be code-defined (closed set + isValidXxxPermission), never a DB-creatable entity, regardless of which RBAC tier. Role catalogs can be DB-dynamic ONLY when no guard/business-invariant hardcodes a specific role name (see org_rbac OWNER implicit-all/anti-lockout hardcode in org.guard.ts:69 and update-role-permissions.handler.ts:22, which is why OrgRole stays a closed enum -- kept unchanged, this refactor only touched System RBAC).]`
  → Removed Permission/RolePermission models entirely (apps/auth-service/prisma/schema.prisma). Role now has a plain permissions String[] column instead of a join table. AssignPermissionsHandler validates codes via isValidSystemPermission(shared-kernel) instead of a PermissionReposi…
- **Graceful shutdown pattern documented as resilience_patterns.md section 5** `[conventions]`
  → Added a proper SIGTERM/SIGINT handler to auth-service's main.ts (order: stop new HTTP+gRPC work -> let in-flight finish, bounded by a 10s forced-exit timeout -> only then disconnect Prisma) after the user asked what startGrpcServer's returned grpc.Server was even for. Documented…
- **Composition Root pattern documented in microservice_architecture.md** `[conventions]`
  → Formalized the fix from the earlier duplicated-Application bug (auth-service main.ts was building buildInfra+buildApplication TWICE -- once inside createApp(), once again just for the gRPC server's CommandBus) as a standing rule in directives/microservice_architecture.md: each P…
- **auth-service composition root built once, shared by HTTP + gRPC (was duplicated per transport)** `[conventions]`
  → User caught it: main.ts originally called buildInfra/buildApplication TWICE in the same process -- once inside createApp() (for the Fastify HTTP transport) and once again separately just to get a CommandBus for the gRPC server. That meant two independent CommandBus instances, ea…
- **Internal gRPC M2M shared-secret auth extracted to shared-kernel (was duplicated per-service)** `[conventions]`
  → User review caught it: the shared-secret check (requireSharedSecret in auth-services grpc-service.ts) and the metadata-set call (in core-apis client) both hardcoded the literal x-internal-secret header name independently -- exactly the duplicate-magic-string class of bug this re…
- **gRPC contracts now codegen via ts-proto (protoc), not hand-typed interfaces** `[conventions]`
  → Follow-up to the org-provisioning gRPC saga (architecture.jsonl same topic, 2026-07-07): the first cut hand-typed the AuthProvisioningClient/Server request+response shapes directly in TS, duplicating proto/org-provisioning.proto by hand -- caught by the user as the obvious weak…
- **First cross-service gRPC call: org provisioning saga (core-api -> auth-service) + POST /orgs locked to System Admin** `[architecture]`
  → Business model change: orgs are onboarded by System Admin post-contract, not self-service. Added apps/core-api -> apps/auth-service synchronous gRPC contract (proto/org-provisioning.proto at repo root, dynamic @grpc/proto-loader, no protoc codegen) — the first request/response i…
- **system-rbac.ts has a comment SUPER_ADMIN không có ở đây guard cấp implicit-all giống OWNER bên org RBAC, and SUPER_ADMIN correctly has zero role_permissions ro…** `[Found while live-verifying the new POST /admin/orgs gRPC org-provisioning flow with the seeded SUPER_ADMIN account (a real login this time, not a hand-crafted JWT) -- see architecture.jsonl org-provisioning-saga entry same date. Lesson: any implicit-all / role-shortcut design that lives only in a JWT-claim-checking guard MUST be expanded at token-mint time, not assumed -- OWNER on the org side works correctly because OrgGuard resolves permissions dynamically per-request from DB role instead of trusting a static JWT claim; System RBAC chose the static-claim design and paid for it here.]`
  → Fixed at the single source: apps/auth-service/src/modules/user/infrastructure/mapper/user.mapper.ts now checks if any of the users roles is SystemRole.SUPER_ADMIN and if so returns [...ALL_SYSTEM_PERMISSIONS] instead of the role_permissions aggregate. One fix point covers both a…
- **User asked to audit ALL permission checks across the app after noticing @RequireSystemPermission('platform:manage_orgs') was a magic string with no catalog, wa…** `[system-permission-catalog-consolidated-shared-kernel]`
- **User-driven monorepo script cleanup, two changes. (1) Consolidated 'npm run dev' and 'npm run dev:stack' into ONE command: added a literal "dev" script (same c…** `[dev-script-consolidation-and-migrate-scripts-removed]`
- **User asked for an objective analysis: does the monorepo's single shared root .env (all 5 services point envFilePath at '../../.env') contradict asymmetric-cryp…** `[env-centralization-tradeoff-analysis-and-env-example-rewrite]`
- **Implemented the split proposed in the earlier env-centralization analysis: user confirmed the design ("shared infra stays in root .env, service-only secrets sp…** `[env-split-implemented-2-source-per-service]`
- **User spotted .env.example had keys (e.g. KAFKA_CONSUMER_MAX_RETRIES) that the real .env didn't -- asked for 3 things: (1) audit all services for still-implicit…** `[env-full-sync-env-vs-example-plus-fill-local-secrets]`
- **User spotted a naming-prefix inconsistency: worker-service's KAFKA_CLIENT_ID had no service prefix, while core-api/notification-service/search-service all use…** `[kafka-client-id-prefix-naming-bug-fixed]`
- **Two live bugs found once user actually ran the stack (infra:up + npm run dev) and opened the FE for the first time. (1) CORS_ORIGINS default in auth-service po…** `[cors-origin-port-mismatch-and-web-missing-dep-fixed]`
- **After fixing the missing react-router-dom dependency via `npm install --workspace=@distributed-social-platform/web`, the page went from a Vite import-analysis…** `[vite-react-dual-version-stale-optimize-deps-cache]`
- **Built apps/web/src/pages/AdminPage.tsx per user request to expose the already-fully-built System RBAC (auth-service /roles+/permissions) and Org RBAC (core-api…** `[admin-page-added-system-and-org-rbac-ui]`
- **MISTAKE during AdminPage live verification: asked user permission to stop 'just the web process' on port 3001 to attach a preview server, framing it as isolate…** `[self-inflicted-full-stack-outage-during-verification]`
- **User correctly called out an IA mistake in the previous AdminPage: System RBAC (platform-wide, auth-service) and Org RBAC (scoped to one org, core-api) were me…** `[split-system-admin-vs-org-admin-with-jwt-permission-guard]`
- **User (Kafka/RAG Section 3 review) spotted 3 real DRY violations via Socratic questioning: (1) DeadLetterInput was byte-for-byte redeclared in notification-serv…** `[dedup-DeadLetterInput-KafkaTopic-and-env-defaults-single-source]`
- **User asked to sweep the WHOLE codebase for the same fallback anti-pattern found in the previous fix (env.config.ts), not just one instance. Grepped `config.get…** `[config-getOrThrow-not-double-default]`
- **Deep-dive teaching session on real-world Kafka operational issues (rebalancing storms, hot partitions, consumer lag, schema-breaking poison pills, replay/repro…** `[kafka-production-hardening-deferred-not-yagni]`
- **Fixed 2 items from the Kafka production-issues deep dive. (1) REAL FIX: docker-init/prometheus/prometheus.yml was missing scrape jobs for core-api(4002)/notifi…** `[prometheus-scrape-gap-fixed-plus-kafkajs-cooperative-sticky-correction]`
- **Phase 5a Credit Ledger = event-sourced wallet, per-user-per-org, fold-on-read (core-api)** `[architecture]`
  → CreditAccount aggregate (domain/aggregates) rehydrate/open/grant/spend/refund/apply; balance folded from credit_events stream — NO summary table (SoT-only, defer read model to Phase 3; overrides event_sourcing.md summary-in-tx guidance for credit). aggregateId = orgId:userId (de…
- **notification-service retrofit: CQRS infra + domain layer (was HTTP→repo direct, no entity)** `[architecture]`
  → Found during code review: notification.controller.ts injected INotificationRepository directly (bypassing Application layer) and the whole notification-service had ZERO CQRS infra (no CqrsModule/CommandBus/QueryBus/TransactionManager) despite cqrs_pattern.md mandating it — grep…
- **shared-kernel has DomainError (statusCode=400) and ApplicationError (custom statusCode+code) both extending AppError, but core-api global-exception.filter.ts o…** `[core-api credit module 5a error design]`
  → For business errors that need proper HTTP codes, extend ApplicationError (precedent: KnowledgeVersionConflictError 409). Credit invariants thrown BY the aggregate live in modules/credit/domain/credit.errors.ts extending ApplicationError — domain→shared-kernel is lint-legal, so n…
- **(1) require(apps/core-api/node_modules/jsonwebtoken) MODULE_NOT_FOUND — workspace hoists it to repo-root node_modules. (2) Another chat`s dev server held :4002…** `[core-api credit 5a verification]`
  → Sign RS256 JWT from ROOT node_modules/jsonwebtoken; private key = base64(JWT_PRIVATE_KEY in .env)→PEM (env.config decodes public key same way). JwtPayload={sub,email,roles,permissions}. To not disturb the running :4002, boot own instance with `CORE_API_PORT=4102 npm run start` —…
- **defineEvent().create({ aggregateId, orgId, payload }) required orgId as a top-level arg, giving false confidence it was persisted/propagated structurally. But…** `[eventing_patterns.md orgId single-source fix, core-api outbox + notification-service/search-service consumers, 2026-07-03]`
  → Added real `orgId` column to OutboxEvent (mirrors CreditEvent/ReputationEvent denormalized orgId pattern) + @@index([orgId]). PrismaOutboxRepository.append() now persists input.orgId. PollingPublisherService reads `event.orgId` (column) instead of payload. Removed orgId from all…
- **IdempotencyInterceptor (core-api, credit spend) ghi idempotency_records TTL 24h nhưng KHÔNG tự xoá → phải có reaper (resilience_patterns §1). Đã thêm Idempoten…** `[idempotency-cleanup-reaper]`
- **apps/web (FE Vite/React) từng embed trực tiếp trong main repo (commit 5aeae23), nay TÁCH thành submodule riêng như 5 service kia. Cách làm non-destructive: git…** `[web-extracted-to-submodule]`
- **Added generalized checklist to directives/eventing_patterns.md §4.1 for choosing aggregateId on new event types: (1) if the aggregate has ONE DB row that survi…** `[aggregateId-selection-rule]`
- **Clarified when a class needs a port/interface vs not, after user asked why PollingPublisherService/OutboxReaperService (apps/core-api/src/modules/outbox/infras…** `[driving-vs-driven-adapter-interface-rule]`
- **Cross-service audit + cleanup after user spotted inconsistent repo/DTO layout in notification-service. Canonical placement (majority = core-api credit/engageme…** `[repo-interface-and-dto-placement-rule]`
- **FINAL cross-service sync of repository-interface placement (supersedes/completes earlier 'repo-interface-and-dto-placement-rule' which had left auth-service un…** `[repo-placement-CANONICAL-cross-service-synced]`
- **Completed the second axis of query-side consistency (after repo-placement): response DTO files are FLAT — one <name>.dto.ts per query-repo at the application/q…** `[query-dto-flat-not-nested]`
- **Phase 4 RAG = search-service (consumer #2, own search_db pgvector + ES)** `[architecture]`
  → Hybrid search microservice, mirror notification-service pattern (own-DB, event-driven, consumer group riêng 'search-service-indexer-group'). C1: embed-on-publish (KnowledgePublished payload += body snapshot, no cross-DB join) → chunk (word window 400/64) → Ollama nomic-embed-tex…
- **directive gọi anthropic.messages.create({max_tokens:1}) làm 'embedding proxy' — trả TEXT không phải vector, vô nghĩa. Không có model claude-embed-*.** `[gotchas]`
  → Embeddings qua provider NGOÀI Claude sau IEmbeddingService. Cortex chọn self-hosted local (Ollama nomic-embed-text dim 768, POST /api/embed {model,input:[...]}→{embeddings:[[...]]}). Claude chỉ dùng summarization (claude-opus-4-8, alias KHÔNG date-suffix). Đã sửa directive §1/§2…
- **(1) Copy notification-service tree → prisma.service.ts còn process.env.NOTIFICATION_DATABASE_URL → health database:error runtime dù typecheck xanh. (2) Boot se…** `[gotchas]`
  → (1) Sau copy tree PHẢI đổi mọi ref DATABASE_URL/service-name (prisma.service, app.module createLogger, health service). (2) Multi-boot: kafka-consumer-groups --delete group + boot 1 bản + chờ stable (describe thấy member, LAG) rồi mới produce (fromBeginning:false). (3) Đổi TEI→O…
- **FE api client luôn set content-type: application/json kể cả request không body (PATCH /notifications/:id/read) → Fastify content-type parser chạy TRƯỚC guard →…** `[apps/web lib/api.ts + notification-service PATCH mark-read]`
  → Client chỉ set content-type: application/json KHI có body: ...(init.body ? {'content-type':'application/json'} : {}). Bài học: smoke bằng curl không thay được browser-verification — header mặc định của client code là khác biệt thật.
- **notification-service-group có committed offset cho knowledge-events nhưng CHƯA TỪNG commit engagement-events → khi service down lúc FollowCreated bắn, boot lại…** `[notification-service consumer group + engagement-events trong E2E]`
  → Với topic mới thêm vào group đang tồn tại: hoặc chấp nhận miss (re-emit bằng hành động mới — user unfollow/follow lại), hoặc reset offset thủ công (kafka-consumer-groups --reset-offsets --to-earliest --topic <new-topic>), hoặc cân nhắc fromBeginning:true cho lần đầu. Kiểm tra CU…
- **FollowCreated keyed by follow.id (random uuid), FollowRemoved keyed by targetId (spaceId) — 2 event của cùng quan hệ follow lạc 2 partition khác nhau → Kafka k…** `[core-api engagement FollowCreated/FollowRemoved → notification-service space_followers projection]`
  → CẢ create lẫn remove key bằng danh tính quan hệ ổn định: Follow.streamKey(userId, targetType, targetId) = `${userId}:${targetType}:${targetId}`. Khớp đúng PK projection [spaceId,userId]. Bài học tổng quát: partitionkey = aggregate identity, không bao giờ dùng row id (nhất là khi…
- **(1) PollingPublisher findMany PENDING không khoá → 2 replica core-api publish trùng. (2) Consumer handler lỗi → throw → kafkajs crash → restart → đọc lại đúng…** `[core-api outbox polling-publisher + notification-service NotificationEventsConsumer]`
  → (1) Claim atomic UPDATE ... status='INFLIGHT' WHERE id IN (SELECT ... FOR UPDATE SKIP LOCKED) RETURNING id; publish NGOÀI tx; Reaper reset INFLIGHT quá timeout về PENDING. Postgres enum cast cần dạng 'INFLIGHT'::"OutboxStatus" (case-sensitive, double-quote); interval dùng make_i…
- **First real consumer = notification-service own-DB (notification_db)** `[notification-service Milestone B1 2026-06-30]`
  → notification-service owns notification_db (NOT core_db). Title snapshot stored at write time (point-in-time copy from event payload) — no cross-DB joins ever. KnowledgeEventsConsumer (kafkajs raw, group notification-service-knowledge-group) → EventRouter → ItemPublishedHandler →…
- **Dựng read model (feed_timeline denormalized + fan-out-on-write projection) khi app chưa xong, chưa có đường đọc nào dùng tới (bảng write-only) — optimization s…** `[core-api + worker-service schema, worker feed module rollback 2026-06-30]`
  → Schema chỉ chứa SOURCE OF TRUTH; query đi thẳng source of truth (join lúc đọc). Read model/projection/denormalize để dành tới READ PHASE và PHẢI nêu thành quyết định để user duyệt, không để trôi như phần hiển nhiên. Đã gỡ feed_timeline + ProcessedEvent + worker feed projection;…
- **Nhầm: muốn 1 event nhiều phản ứng thì nhồi nhiều handler vào 1 EventRouter (router 1:N)** `[worker-service EventRouter, knowledge-events.consumer, eventing_patterns.md]`
  → Kafka-native: fan-out 1 event → N concern = N consumer GROUP riêng (mỗi concern 1 consumer + router 1:1, group id riêng theo concern). Router giữ 1:1, register() ném khi trùng type (guard). EventRouter scope per-module (rẻ, Map in-mem), KHÔNG global; KafkaClientService mới singl…
- **Event/messaging layer ráp ad-hoc, không neo vào pattern có tên → khó maintain, thiếu bản đồ** `[shared-kernel events+messaging, core-api outbox, worker-service feed]`
  → Chốt reference architecture trong directives/eventing_patterns.md: wire contract = CloudEvents 1.0 (CloudEvent<T>); transport selection = binder pattern (EVENT_TRANSPORT_MAP + CompositeMessagePublisher, Kafka+queue coexist); inbound = Message Dispatcher (EventRouter map type->ha…
- **payload: unknown ở outbox append + envelope tự chế tên field (eventId/payload) → mismatch eventType-payload không bắt được lúc compile, không interop** `[shared-kernel/events, core-api PollingPublisher + publish-knowledge.handler]`
  → Typed event factory: mỗi event 1 file events/definitions/<event>.event.ts gom payload type + defineEvent({eventType, aggregateType}); producer gọi .create({aggregateId,orgId,payload}) → compile-error nếu thiếu/sai field. Wire envelope = CloudEvents 1.0 chuẩn (id/source/type/time…
- **notification-service B2: FOLLOW events + fan-out** `[architecture]`
  → First real microservice with own-DB + event-driven local projection. FollowCreated/FollowRemoved emitted from core-api EngagementModule (transactional outbox, transactional:true). notification-service keeps space_followers as local projection (upsert/delete by PK [spaceId,userId…
- **Feed read endpoint query thẳng SoT (join follows×knowledge_items), không read model. Fan-out-on-read: bước 1 lấy spaceIds từ follows WHERE targetType=SPACE; bư…** `[feed-read-query-sot]`
- **Phase 2a: Transactional Outbox + Polling Publisher hoàn chỉnh trong core-api. Event contracts ở shared-kernel làm single source of truth.** `[apps/core-api/src/modules/outbox/, packages/shared-kernel/src/events/, apps/core-api/src/modules/knowledge/application/commands/publish-knowledge/, directives/resilience_patterns.md §Transactional Outbox]`
  → Dual-write nguy hiểm: handler save DB rồi publish Kafka → giữa 2 lệnh process chết → inconsistency. Outbox giải quyết bằng cách ghi domain row + outbox row trong CÙNG 1 DB transaction (nhờ getTx() AsyncLocalStorage). PollingPublisher chạy @Interval(2000) đọc PENDING rows → push…
- **Phase 2b: worker-service scaffold + FeedTimeline fan-out consumer hoàn chỉnh. 5 gotchas cốt lõi cần ghi nhớ.** `[apps/worker-service/src/, apps/core-api/prisma/schema.prisma (ProcessedEvent, FeedTimeline @@unique), packages/shared-kernel/src/events/]`
  → (1) NestFactory.createApplicationContext NOT NestFactory.create — worker không HTTP. (2) kafkajs raw (không @nestjs/microservices) → eachMessage callback, manual group worker-feed. (3) ProcessedEvent model PHẢI đặt ở core-api prisma schema (owns core_db) — worker schema chỉ để t…
- **Log field `context` taxonomy: cross-service shared contexts (CQRS middleware CommandBus/RetryMiddleware/TransactionMiddleware, HttpLayer, ExceptionFilter) khai…** `[packages/shared-kernel/src/logger/{index.ts,log-context.ts}, packages/shared-kernel/src/cqrs/middleware/{logging,retry,transaction}.middleware.ts, apps/*/src/infrastructure/http/{interceptors,hooks,filter}, directives/logging_standard.md §context taxonomy]`
  → CQRS middleware tự đính this.logger.info({context: LogContext.X}, msg) dạng object-first — KHÔNG cần set context ở composition root, core-api vẫn giữ requestId (PinoLogger lấy per-request logger từ AsyncLocalStorage lúc log), auth-service nhận field qua root. App-local single-se…
- **Log verbosity KHÔNG đồng đều giữa 3 CQRS bus — tỉ lệ với (tác động ghi × giá trị audit), nghịch với tần suất. CommandBus: info lifecycle (executing→success+dur…** `[packages/shared-kernel/src/cqrs/{query-bus,event-bus,command-bus}.ts, packages/shared-kernel/src/logger/log-context.ts, apps/auth-service/src/container/application.ts, apps/core-api/src/infrastructure/cqrs/cqrs.module.ts, directives/logging_standard.md §Buses & logger]`
  → EventBus/QueryBus BẮT BUỘC truyền logger ở composition root: auth new XxxBus(infra.logger); core useFactory (logger: PinoLogger) => new XxxBus(logger) inject [PinoLogger]. CommandBus vẫn useValue new CommandBus() (không logger). ⛔ KHÔNG copy info executing/success của CommandBus…
- **Log secret masking 2 lớp: (1) Root logger createLogger có pino `redact` (in-process trước mọi transport) mask password/token/refreshToken/authorization/cookie…** `[packages/shared-kernel/src/logger/index.ts (LOG_REDACT_PATHS, createLogger redact), packages/shared-kernel/src/logger/redact.spec.ts, packages/shared-kernel/src/cqrs/middleware/logging.middleware.ts, directives/logging_standard.md §Bảo mật log]`
  → Vì sao 2 lớp: dump nguyên command có (a) secret (LoginCommand/RegisterCommand.password, RefreshCommand.refreshToken) → redaction lo; (b) volume (UpdateKnowledgeCommand.body) → debug-level lo. Thêm secret field mới (apiKey...) PHẢI thêm path vào LOG_REDACT_PATHS, không dựa 'nhớ đ…
- **@/ path alias not resolved in dist — .js extension on alias import** `[apps/worker-service — all src files with @/ imports]`
  → NEVER add .js extension to @/ alias imports. Write `from '@/infrastructure/prisma/prisma.service'` not `from '@/infrastructure/prisma/prisma.service.js'`. TSC emits correct relative paths in dist when no extension is present on alias imports.
- **Gọi createLogger() ad-hoc ở module scope trong feature code (vd const logger = createLogger('kafka-producer') trong kafka-producer.service.ts) tạo thêm pino tr…** `[apps/core-api/src/modules/outbox/infrastructure/{kafka-producer,polling-publisher}.service.ts, apps/core-api/src/infrastructure/cqrs/cqrs.module.ts (inject PinoLogger), packages/shared-kernel/src/logger/index.ts, directives/logging_standard.md §Logger Hierarchy]`
  → ROOT logger tạo 1 lần ở composition root (core-api: LoggerModule.forRootAsync logger: createLogger('core-api') trong app.module.ts). Mọi component khác INJECT child logger: @InjectPinoLogger(XxxService.name) private readonly logger: PinoLogger (nestjs-pino) — chung 1 transport c…
- **EventBus (shared-kernel in-process emitter) dùng console.error trong handler error path — vi phạm rule no-console, log không structured, không có context/servi…** `[packages/shared-kernel/src/cqrs/event-bus.ts, packages/shared-kernel/src/logger/log-context.ts (EVENT_BUS), apps/auth-service/src/container/application.ts, apps/core-api/src/infrastructure/cqrs/cqrs.module.ts, directives/logging_standard.md §Buses & logger]`
  → EventBus BẮT BUỘC new EventBus(logger: ILogger). Event in-process fire-and-forget KHÔNG đi qua middleware pipeline (khác CommandBus có LoggingMiddleware) nên EventBus tự catch lỗi handler + log this.logger.error({context: LogContext.EVENT_BUS, eventName, err}, 'Error handling ev…
- **NestJS ConfigModule không tìm thấy .env khi run từ apps/core-api (monorepo)** `[errors]`
  → Đổi envFilePath thành '../../.env' (relative to apps/core-api CWD). Prisma đã làm đúng qua prisma.config.ts; NestJS ConfigModule phải mirror cùng path. Pattern này áp cho MỌI service trong monorepo: apps/<service>/ CWD → '../../.env' để tìm root .env
- **core-api modules/tenant entities (Membership/Organization/Space/OrgInvite) viết theo style immutable + props-bag (private readonly props; changeRole/accept ret…** `[core-api tenant domain entities; canonical entity style = mutable + individual fields (NOT immutable/props-bag)]`
  → Realign core-api -> canonical = MUTABLE + individual private _fields (giống auth role.entity.ts / refresh-token.entity.ts): (1) gán từng field this._x = props.x trong constructor, KHÔNG props-bag; Date clone phòng thủ new Date(getTime()). (2) Behavior method mutate IN-PLACE trả…
- **auth-service entities (canonical reference) còn sạn style dù đã mutable+individual-fields: (1) getter đặt tên 'get getX()' (user.getRoles/getPermissions/getPro…** `[auth-service + core-api domain entities; refine canonical style = getter naming + clone-both-directions + entity owns identity]`
  → (1) Bỏ prefix: get roles()/permissions()/profile()/authIdentities() (đổi hết call-site mapper/repo/handler/spec). (2) Clone CẢ HAI CHIỀU cho field mutable: getter array trả [...this._x], getter Date trả new Date(this._x.getTime()) (null-safe); primitive + child-entity (profile t…
- **core-api: enforce Clean-Arch layer boundaries bằng @typescript-eslint/no-restricted-imports per layer (domain/application/presentation/common) trong eslint.con…** `[apps/core-api/eslint.config.mjs; directives/folder_structure_sop.md §Enforcement; directives/multi_tenancy.md Forbidden Patterns; commits 49126f3 + c3bea99 + a8a7675]`
  → Gốc rễ: prior NestJS mâu thuẫn hexagonal directive (guard query DB thẳng, handler throw HttpException, @Injectable khắp nơi) — nơi LLM lệch nhiều nhất; auth-service Fastify thuần sạch hơn vì không có idiom framework cãi lại. Doc mô tả INTENT nhưng không ràng buộc OUTPUT nên tái…
- **core-api modules/tenant (AI code 100%) vi phạm hexagonal dù tài liệu đầy đủ: OrgGuard/TenantInterceptor đặt trong common/ (HTTP infra), OrgGuard inject PrismaS…** `[core-api modules/tenant layering re-audit + eslint boundary enforcement]`
  → Di dời guard/interceptor về infrastructure/http/{guards,interceptors}; guard đi qua IMembershipRepository; throw ApplicationError subclass (MembershipNotFoundError) thay HTTP exception; chuyển org-permissions về modules/tenant/domain/; tách OrgContext sang infrastructure/http/ty…
- **Entity factories anemic + sai ranh giới validate: create() nhận discriminator (role) tự do không rule (leo thang OWNER), và mapper/rehydrate RE-VALIDATE persis…** `[core-api tenant entities (Membership/OrgInvite/Organization/Space) + auth rbac; domain factory + persistence mapper boundary]`
  → (1) Factory intention-revealing enforce invariant lúc tạo: createOwner vs createMember(ManageableOrgRole), createForRegister(hash password). Bất biến bảo mật dùng TYPE compile-time (ManageableOrgRole = Exclude<OrgRole,'OWNER'>) hơn runtime guard. (2) Validate khi GHI (factory +…
- **RetryMiddleware: thêm full jitter + maxDelayMs cap (2000); rà soát toàn bộ CommandOptions của 18 command; xác nhận retry hiện dormant, load-bearing từ Phase 5;…** `[packages/shared-kernel/src/cqrs/middleware/retry.middleware.ts, apps/*/src/infrastructure/database/prisma/prisma-transient-error.ts, apps/*/src/infrastructure/cqrs/cqrs.module.ts, directives/resilience_patterns.md §3, task NEXT: knowledge-module OCC]`
  → Retry chỉ fire khi P2034/P2028 — ở quy mô hiện tại gần như không xảy ra nên là dead code path. Trở thành load-bearing từ Phase 5 (Event Sourcing OCC + Credit Economy) và Phase 2 (Outbox khiến retry-safe vững chắc). Thứ tự Retry-wraps-Transaction đã đúng cho OCC: retry chạy lại h…
- **Org context được truyền qua x-org-id header thay vì nhúng orgId vào JWT access token** `[apps/core-api/src/common/tenant/org.guard.ts, apps/auth-service/src/modules/auth/infrastructure/services/imp-token.service.ts]`
  → auth-service không cần biết về org. core-api OrgGuard đọc x-org-id header, query DB (membership table) lấy role → inject OrgContext vào request. Không cần re-issue token khi đổi org. JWT chỉ mang system-level identity: {sub, email, roles, permissions}. · alt: Nhúng orgId vào JWT…
- **AI workflow gap fix: bổ sung docs/*.md vào scope cập nhật sau mỗi task, thêm format 'decision' vào memory entry** `[AGENTS.md After-Task Protocol, directives/memory_sop.md, docs/11_auth_service_review.md]`
  → AGENTS.md After-Task Protocol chỉ mention directives/*.md — khiến docs/ (review findings, API contracts) bị stale khi code đã giải quyết vấn đề. Thiếu format log cho quyết định thiết kế chủ động (chỉ có error/solution, không có decision/rationale). · alt: Không thay đổi — loại v…
- **§4 quick-ref tách khỏi builder.py → .ai/QUICK_REFERENCE.md (curated, inject); sync.cjs Stop-hook mở rộng trigger sang .ai/PROJECT_STATUS + .ai/QUICK_REFERENCE;…** `[scripts/sync.cjs, .ai/QUICK_REFERENCE.md, SETUP.md, knowledge_builder.py]`
  → Quick-ref hardcode trong Python = frozen duplicate, stale âm thầm như docs/11. sync.cjs đã regenerate index khi directives/docs/.ai/memory đổi nhưng bỏ sót 2 file curated mới → edit chúng không auto-refresh §2/§4. SETUP.md lưu PHƯƠNG PHÁP dựng codebase này (Cortex là project THẬ…
- **sync.cjs Stop-hook thêm 2 warn-only check tất định: (A) code apps|packages/*/src/*.ts đổi mà memory/PROJECT_STATUS không mới hơn (so mtime, vì memory gitignore…** `[scripts/sync.cjs checkWorktree + checkDiscipline, SETUP.md §5c]`
  → Phát hiện cả 2 hook (doc-select inject reminder + After-Task prose) là loại 'nhắc rồi hy vọng agent tuân' = mềm, bỏ được — đã đứt thật trong session. Tệ hơn: session ngồi ở worktree (cwd) nhưng work ở main → sync.cjs git-status worktree luôn rỗng → no-op IM LẶNG cả session. Nguy…
- **HTTP layer response shape (error body, success body, status-to-code mapping) duplicated across auth-service hooks và core-api interceptors/filters gây drift th…** `[shared-kernel http utilities, auth-service hooks, core-api interceptors/filters]`
  → Extract 3 utility functions vào shared-kernel: httpStatusToCode(), buildErrorBody(), buildSuccessBody(). Cả hai service dùng chung — hook/interceptor/filter chỉ còn là thin wrappers gọi shared-kernel. Xem directives/logging_standard.md và directives/microservice_architecture.md.
- **shared-kernel/src/http/ có 6 file rời rạc, types bị tách khỏi classes liên quan (SuccessResponse ở build-success-body.ts, HttpResponseBuilder ở success-respons…** `[shared-kernel http layer refactor]`
  → Gộp thành 2 file: response.ts (tất cả types+classes: BaseMeta, ErrorResponse, SuccessResponse, HttpResponseBuilder, HttpResponseError) và response.utils.ts (tất cả factory functions: buildErrorBody, buildSuccessBody, httpStatusToCode). Xoá 6 file cũ.
- **HttpResponseBuilder naming sai (không phải Builder Pattern) và HttpResponseError nằm trong http/ nhưng extends ApplicationError từ errors/ — sai chiều dependen…** `[shared-kernel http layer naming + structure]`
  → Rename HttpResponseBuilder → ApiResponse (data class, không phải builder). Move HttpResponseError → ResponseFormatError extends InfrastructureError vào errors/response-format.error.ts. http/response.ts giờ chỉ còn pure contracts (interfaces + ApiResponse data class). Dependency…
- **Bootstrap audit — auth-service & core-api (2026-06-20): Tất cả vấn đề bootstrap đã được fix. auth-service: logger conditional bug (FST_ERR_LOG_LOGGER_AND_LOGGE…** `[architecture]`
- **shared-kernel http/ refactor: 6 file fragmented → 2 file clean. response.ts: ApiResponse class + SuccessResponse/ErrorResponse interfaces + BaseMeta. response.…** `[architecture]`
- **Services tự inline response body thay vì dùng shared-kernel utilities.** `[HTTP response shape convention]`
  → Convention: NEVER inline { success, message, error, meta } trực tiếp trong hook/filter/interceptor. LUÔN dùng buildErrorBody() / buildSuccessBody() / httpStatusToCode() từ @distributed-social-platform/shared-kernel. Đây là Single Source of Truth cho HTTP response shape.
- **core-api thiếu /health và /metrics endpoint, HttpLoggingInterceptor log noise cho những route này.** `[core-api health/metrics endpoints]`
  → 1. HttpLoggingInterceptor: skip nếu req.url === '/health' hoặc '/metrics'. 2. HealthController tại src/infrastructure/http/controllers/health.controller.ts dùng @Res() để bypass ResponseInterceptor. 3. server.ts: setGlobalPrefix với { exclude: ['health', 'metrics'] }. 4. Fastify…
- **Thiếu genReqId trong core-api FastifyAdapter — requestId không propagate từ x-request-id header của upstream.** `[core-api bootstrap/server.ts distributed tracing]`
  → Thêm genReqId: (req) => { const h = req.headers['x-request-id']; return Array.isArray(h) ? h[0] : (h ?? randomUUID()) } vào FastifyAdapter constructor trong server.ts. Pattern này đồng bộ với auth-service.
- **auth-service /health và /metrics route trả về 500 vì httpResponseHook throw ResponseFormatError khi nhận plain object hoặc string (không phải ApiResponse).** `[auth-service bootstrap/server.ts]`
  → Thêm { config: { skipResponseWrapper: true } } vào route options cho /health và /metrics trong auth-service server.ts. Fastify sẽ skip httpResponseHook cho những route này.
- **core-api env.validation.ts thiếu JWT_PUBLIC_KEY và CORE_DATABASE_URL — service start thành công dù thiếu biến env quan trọng.** `[core-api config]`
  → Thêm CORE_DATABASE_URL: z.string().url() và JWT_PUBLIC_KEY: z.string().min(100) vào envValidationSchema. Bỏ || '' fallback trong env.config.ts — dùng process.env.JWT_PUBLIC_KEY! sau khi đã validate.
- **Anemic Domain Model: Handler created empty entity then called update() to fill data** `[DDD Entity Creation Lifecycle]`
  → Intention-Revealing Interfaces (Factory Pattern): Entity creation must be atomic. Use Factory methods on the Entity (e.g. `createPremiumUser`, `createForLogin`, or pass initial data directly to `create`) instead of creating a hollow entity and letting the Application layer (Hand…
- **Entity creation used undefined instead of null, causing inconsistent mapping** `[Domain Entity Definitions]`
  → Entity Properties Nullability Convention: Entity properties mapping to DB columns MUST use `null` (not `undefined`) for optional/empty fields. This enforces explicit declaration of missing data. The `update()` method (Partial Update pattern) should use `?: type | null` where `un…
- **Inconsistent usage of deletedAt vs isActive causing ambiguity between 'Temporarily Disabled' and 'Soft Deleted'** `[Prisma Schema Design and Soft Deletion Strategy]`
  → Database Schema Convention: (1) Use 'isActive' for temporary toggle/disable (e.g. banning user, disabling role). (2) Use 'deletedAt' for Soft Delete (user-initiated delete, removing role permanently but keeping audit history). (3) Use Hard Delete (neither field) for mapping/join…
- **Repeated Offense (3rd time): Throwing generic 'throw new Error()' in Application layer Handlers instead of specific Domain ApplicationErrors** `[RBAC Module Handlers]`
  → NEVER use generic 'throw new Error()' inside CQRS Handlers. You MUST create custom error classes (e.g., RoleAlreadyExistsError, PermissionNotFoundError) extending ApplicationError in src/common/errors/ and throw them. This guarantees global filters can catch and format HTTP resp…
- **Inconsistent API routes regarding Swagger documentation, Authentication, and Zod Schema validation** `[Fastify Routing Conventions (fastify-type-provider-zod + fastify-swagger)]`
  → Follow Fastify Enterprise Pattern: Separate Swagger metadata (`description`, `tags`, `security: [{ cookieAuth: [] }]`) from Zod Validation (`...schema`). Place security enforcement in `preHandler` (e.g., `fastify.authenticate`). This ensures correct Swagger UI (locks) and proper…
- **Props interface pass-by-reference leak in Domain Entity constructors breaking OOP Encapsulation** `[Domain Entity Constructors]`
  → Always clone the props object in the Entity constructor (e.g. `this.props = { ...props }`) instead of using `private readonly props: EntityProps` directly as a parameter property, to prevent external reference leaks.
- **Domain entities declared using primitive properties in their constructor instead of a strongly typed Props interface, leading to parameter ordering bugs and la…** `[Domain Entity structure across all modules (e.g. User, Permission, Role, Expense)]`
  → Always define a `[EntityName]Props` interface and use `private constructor(private readonly props: [EntityName]Props)` for Domain Entities. Use `Omit<Props, ...>` in factory methods like `create()` for clarity.
- **Query logic duplicated across multiple Handlers and DTOs (e.g. get-profile vs get-me)** `[auth-service get-profile and get-me redundancy]`
  → Strictly enforce DRY for Query/Read Models. If two queries return the exact same DTO and use the same infra logic, merge them into a single Query/Handler.
- **Schema and HTTP files poorly named (e.g., me.schema.ts, user.http) violating 1-1 mapping with Handlers** `[auth-service presentation layer naming]`
  → Schema files, HTTP test files, and Handlers must map 1-1 in naming convention (e.g., get-me.handler.ts -> get-me.schema.ts -> get-me.http). Do not group multiple requests into generic files.
- **Agent creating complex domain files before user explicitly approves the Implementation Plan (running ahead of the car)** `[Agent Etiquette during Expense Module planning]`
  → Always wait for explicit user approval (a clear 'yes' or 'approve') before executing any new Implementation Plan.
- **Infrastructure Leakage: Query Handlers directly importing PrismaClient** `[GetProfileHandler using PrismaClient]`
  → CQRS Query Handlers must depend on QueryRepository interfaces defined in the application layer, not infrastructure details like Prisma.
- **Exception Handling: Throwing generic Error in Handlers instead of Domain Errors** `[GetProfileHandler throwing new Error()]`
  → Always use custom errors inheriting from ApplicationError (e.g., UserNotFoundError) so global exception filters can catch them.
- **Query handlers reusing Domain Repositories (UserRepository) violates strict CQRS by fetching Entities instead of DTOs, and creating unused methods like findByI…** `[CQRS architecture in modules/<domain>]`
  → Strictly separate Query vs Command Repositories. Put Command repos in domain/repositories/ (returning Entities) and Query repos in application/repositories/ (returning DTOs). Flatten infrastructure/repositories/ without commands/queries subfolders.
- **Used '-it' flag in docker exec causing TTY error. Skipped memory search before complex planning. Forgot to read directives/ SOPs before planning.** `[conventions]`
  → Never use '-it' for automated docker exec scripts. Always run memory_manager.py search before making plans (Rule #2). Always read relevant SOPs in directives/ before creating implementation plans to properly cite them in 'References & Compliance' (Rule #3).
- **FST_ERR_LOG_LOGGER_AND_LOGGER_INSTANCE_PROVIDED: You cannot provide both logger and loggerInstance** `[apps/auth-service/src/bootstrap/server.ts]`
  → When configuring Fastify logger, do not include both 'logger' and 'loggerInstance' in the options object (even if one is undefined). Use object spread to conditionally include only one: ...(isTest ? { logger: false } : { loggerInstance: createLogger() })
- **FST_ERR_LOG_INVALID_LOGGER_CONFIG: logger options only accepts a configuration object** `[apps/auth-service/src/bootstrap/server.ts]`
  → In Fastify v5, the 'logger' option only accepts a boolean or configuration object. To pass a custom Pino logger instance, use the 'loggerInstance' option instead of 'logger'.
- **Mở quyền Read-Write (:rw) cho Sandbox để tự động sửa lỗi, vô tình phá vỡ lớp bảo vệ an toàn Layer 0.** `[docker-compose.agent.yml, tool_builder_sop.md, Kiến trúc bảo mật]`
  → Thiết lập mô hình 'Circuit Breaker': Sandbox chỉ được chạy script ở chế độ Read-Only và xuất file JSON/Patch ra thư mục .tmp/. Sau đó AI (Agent) đóng vai trò người kiểm duyệt, tự đọc report và sửa mã nguồn bằng công cụ native của mình.
- **Framework code (NestJS modules) nằm lẫn trong common/cqrs/ phá vỡ quy tắc Pure POJO của CQRS Bus.** `[common/cqrs, infrastructure/cqrs]`
  → Bắt buộc đẩy các class có @Injectable(), @Module() (ví dụ: cqrs.module.ts) ra khỏi common/ và đưa vào infrastructure/cqrs/. Các Bus trong common/ phải là Pure TypeScript.
- **Không chủ động cập nhật tài liệu (SOP) ngay khi có quyết định kiến trúc mới (CQRS Pure POJO) mà chỉ tập trung code.** `[directives/memory_sop.md, CQRS Module]`
  → Đưa việc cập nhật file .md trong thư mục directives/ và log memory trở thành bước BẮT BUỘC và TỰ ĐỘNG ngay lập tức mỗi khi có rule mới, không đợi user nhắc.
- **Middlewares in CQRS CommandBus apply globally, causing unnecessary Transaction/Retry overhead on commands that don't need them.** `[architecture]`
  → Added CommandOptions to ICommand interface (transactional, retryable). Each Command declares its own needs. Middlewares check command.options at runtime and call next() immediately if not opted in. Infra-specific logic (isPrismaTransientError, PrismaTransactionManager) is inject…
- **Prisma transactions inside CQRS Middlewares break Hexagonal Architecture because Repositories require the 'tx' transaction client parameter explicitly, alterin…** `[architecture]`
  → Implemented AsyncLocalStorage for PrismaTransactionClient. The TransactionMiddleware initiates prisma. and saves 'tx' via AsyncLocalStorage context. PrismaRepositories simply use getTx() || this.prisma, completely decoupling infra transactions from the domain interface.
- **Basic CommandBus lacks cross-cutting concerns (Middleware) like Logging/Transactions, leading to duplicated code in handlers.** `[architecture]`
  → Implemented Onion Architecture Middleware Pipeline in CommandBus. execute(command) now uses recursive dispatch. Created ICommandMiddleware interface. Added LoggingMiddleware as proof of concept using the central logger.
- **Prisma schema validation - Error code: P1012: The datasource property url is no longer supported in schema files (Prisma v7)** `[Prisma v7 upgrade issue]`
  → Delete url=env() from schema.prisma and ensure prisma.config.ts has datasource: { url: process.env.DATABASE_URL }
- **Code Quality: Repository using fake setTimeout implementation instead of real DB calls** `[core-api Infrastructure repository implementation]`
  → Implement real Prisma calls in prisma-user.repository.ts and inject PrismaService
- **DevOps: Services crash because Kafka or Elasticsearch haven't fully started** `[docker-compose dependency management]`
  → Add healthcheck configurations for Kafka and Elasticsearch in docker-compose.yml
- **Architecture: Microservice registered with wrong name 'api-gateway' instead of 'core-api'** `[core-api microservice communication]`
  → Fix service name registration in app.module.ts ClientsModule to match actual service name
- **Code Quality: Accessing private entity properties via bracket notation (e.g., user['profile'])** `[auth-service user.mapper.ts and user.entity.ts]`
  → Implement explicit getter methods (getProfile(), getAuthMethods()) in the Entity class
- **Code Quality: Magic numbers used for token expiration times** `[auth-service token generation]`
  → Extract magic numbers into constants (e.g. ACCESS_TOKEN_TTL) in imp-token.service.ts
- **HTTP Protocol: Token validation failures returning HTTP 400 Bad Request** `[auth-service error handling]`
  → Change HTTP status code to 401 Unauthorized for all Token-related errors in auth.error.ts
- **Security/Runtime: fastify.jwt.decode() returning null causing crashes when accessing properties** `[auth-service JWT decoding in auth.routes.ts]`
  → Add null safety guard `if (!decoded?.sub)` before accessing token payload
- **Security: CORS configured with wildcard origin ['*']** `[auth-service fastify.ts security config]`
  → Load CORS origins from environment variables instead of hardcoding wildcard
- **Entity data loss: rehydrate() method in User entity drops profile data** `[auth-service Domain Entity rehydration]`
  → Update the rehydrate() factory method to include profile in the recreated instance
- **Prisma: Missing DATABASE_URL env var during generation or runtime in auth-service and core-api** `[Prisma schema configuration]`
  → [SUPERSEDED by #16] Add `url = env("DATABASE_URL")` explicitly to the datasource block in schema.prisma
- **Domain Entity: ensureCanLogin throw error chung chung Error()** `[Domain logic auth-service]`
  → Tạo UserCannotLoginError trong auth.error.ts và sửa lại entity
- **Jest SyntaxError: Unexpected token 'export' khi import ESM từ shared-kernel** `[Unit tests auth-service]`
  → Bỏ isolatedModules trong ts-jest config package.json, dùng diagnostics.ignoreCodes: [151002] thay thế
- **Prisma: Unique constraint failed on fields (userId, provider) instead of just userId** `[auth-service Prisma schema AuthMethod]`
  → Sửa @unique thành @@unique([userId, provider]) trong schema.prisma
